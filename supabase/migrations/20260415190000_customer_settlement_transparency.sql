-- This migration restores the historical version stamp used by the remote
-- project for the customer settlement transparency update.
--
-- The schema export already contains the settlement tables, policies, and
-- supporting functions, so this file is a safe no-op placeholder to align the
-- local migration history with the linked project.

SELECT 1;

