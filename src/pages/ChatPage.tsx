import ChatWorkspacePage from '@/features/chat/pages/ChatWorkspacePage';

/**
 * ChatPage - Modernized Merchant Trading OS Layout
 * This is now a wrapper around the feature-driven ChatWorkspacePage.
 * Enforces 4-column viewport-locked architecture.
 */
export default function ChatPage() {
  return <ChatWorkspacePage />;
}
